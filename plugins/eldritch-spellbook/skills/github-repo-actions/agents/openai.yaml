interface:
  display_name: "GitHub Repository Actions"
  short_description: "Handle explicit GitHub actions with safe boundaries."
  default_prompt: "Use $github-repo-actions to handle this requested GitHub action."

policy:
  allow_implicit_invocation: false
